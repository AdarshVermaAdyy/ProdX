import { GroupNamePipePipe } from './group-name-pipe.pipe';

describe('GroupNamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new GroupNamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
